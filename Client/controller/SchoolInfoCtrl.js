var app = angular.module('sfApp.schoolInfoRegister',['sfApp.sInfoFactory']);

app.controller('schoolInfo',function($scope,schoolInfoFactory)
{
	//alert("helo");

   // $scope.GetSignIn = function () {
   // 	//alert($scope.form);
   //   registerFactory.SaveRegister($scope.form).success(function (resultData) {
   //          //$scope.success = resultData;          
   //      }).error(function (errorData) { });
  

    //};

});
